---
- name: Registry
  hosts: download
  become: true
  tasks:
    
    # Get Kolla-Image List
    - ansible.builtin.import_role:
        name: get-image-list-kolla

    - name:
      debug:
        var: specific_tag

    # Pull Image 
    - ansible.builtin.import_role:
        name: service-images-pull
